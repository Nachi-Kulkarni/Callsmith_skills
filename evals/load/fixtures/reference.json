{
  "worker": ["node", "evals/load/fixtures/reference-worker.mjs"],
  "concurrency": 8,
  "signal_after_turns": 1,
  "max_p95_degradation_pct": 25,
  "timeout_ms": 15000,
  "notes": "CI proof config, run from repo root: the reference worker drains correctly, so the gate must PASS it. Worker paths are repo-root relative."
}
